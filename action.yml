name: 'Setup Nuget'
description: 'Goes and downloads latest Nuget.exe as CLI tool and puts it into the path with support to UNIX environment using Mono.'
author: 'Rickedb'
runs:
  using: 'node12'
  main: 'lib/main.js'
branding:
  icon: 'download'
  color: 'purple'
